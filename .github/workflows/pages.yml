name: Deploy docs to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate results (only if FDA_API_KEY is set)
        if: ${{ secrets.FDA_API_KEY != '' }}
        env:
          FDA_API_KEY: ${{ secrets.FDA_API_KEY }}
        run: |
          python src/main.py

      - name: Generate results (skipped: no API key)
        if: ${{ secrets.FDA_API_KEY == '' }}
        run: |
          echo "FDA_API_KEY not set — skipping results generation. Using checked-in docs/ if present."

      - name: Sync results
        run: |
          python scripts/sync_results.py || true

      - name: Ensure docs exist
        run: |
          mkdir -p docs/results
          ls -la docs || true

      - name: Build site (no-op)
        run: echo "No static site build step required; using docs/ contents"

      - name: Upload to Pages
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1
